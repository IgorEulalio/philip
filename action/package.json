{
  "name": "philip-action",
  "version": "0.1.0",
  "private": true,
  "description": "Philip Supply Chain Detector GitHub Action",
  "scripts": {
    "build": "npm run build:index && npm run build:post",
    "build:index": "ncc build src/index.ts -o dist",
    "build:post": "ncc build src/post.ts -o dist-post && mv dist-post/index.js dist/post.js && rm -rf dist-post"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
